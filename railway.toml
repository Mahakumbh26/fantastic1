[build]
builder = "NIXPACKS"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[observability]
# Railway will scrape this endpoint for Prometheus metrics
metricsPath = "/metrics"
